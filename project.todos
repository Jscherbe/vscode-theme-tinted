Todos:
  ✔ Need to work on GIT merge colors @done
    ✔ Not sure if we can change color of "Incoming Change" label, may need to change background color @done
    * Note: The color for that is shared across the ui, I tweaked it slightly
